
ARTEFACT_DIR=~/.lmstudio/models
CHECKSUM=$(readlink -f models.sha256sum)

cd $ARTEFACT_DIR
if ! test -f $CHECKSUM ;then
    find . -type f |  xargs -I {} sha256sum {} > $CHECKSUM
fi
sha256sum --check --quiet --ignore-missing $CHECKSUM
