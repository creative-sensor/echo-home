declare -a path_dict

path_dict["rtfunction"]="$HOME/echo-home/.function"
path_dict["mcfunction"]="$HOME/echo-home/workstation/macos/.function"


for key in "${!path_dict[@]}"; do
  PATH+=":${path_dict[$key]}"
done

unset path_dict
