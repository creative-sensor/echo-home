path_dict=("mcfunction" "$HOME/echo-home/workstation/macos/.function")

path_dict["rtfunction"]="$HOME/echo-home/.function"
path_dict["yson"]="$HOME/echo-home/codev/yson/.function"
path_dict["gnupg"]="$HOME/.local/bin/gnupg/bin:$HOME/.local/bin/pinentry/bin"
path_dict["gmlama"]=$LOCAL_BIN/gmlama

for key in "${!path_dict[@]}"; do
  PATH+=":${path_dict[$key]}"
done

unset path_dict
