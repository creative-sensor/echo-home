# DNS Management System

This system provides tools for managing local DNS entries in the `/etc/hosts` file with metadata.

## Features

- Add DNS entries with labels, tags, and comments
- Remove entries by label
- List all managed entries
- Preserves existing native entries
- Only manages what it inserts/removes

## Usage

### Add an entry
