declare -A path_dict

path_dict["rtfunction"]="$HOME/echo-home/.function"
path_dict["gmlama"]=$LOCAL_BIN/gmlama


for key in "${!path_dict[@]}"; do
  PATH+=":${path_dict[$key]}"
done

unset path_dict
