### .function
```
.function/curl-auth
    Attempt to get authenticated
.function/useradd
    Add new user in local db file
.function/start
    Start server
```

### .properd
```
.properd/authelia.env
    ENV vars to override those in configuration.yaml
.properd/configuration.yml
    main configuration
.properd/meta.yaml
.properd/users_database.yml
    list of users who can get authenticated
.properd/server.tls.key.pem
    Prebuilt TLS key for server
.properd/server.tls.certificate.pem
    Prebuilt TLS certificate for server
.properd/ca.pem
    CA cert
.properd/VARSET
    objectory interface
```

