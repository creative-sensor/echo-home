PROFILE=${MOZILLA_THUNDERBIRD_PROFILE}
    # Path to encrypted profile

cd '/c/Program Files/Mozilla Thunderbird'
test -z "$PROFILE" && echo "NO PROFILE FOUND"
test -n "$PROFILE" && ./thunderbird.exe --profile $PROFILE

