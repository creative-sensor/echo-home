ARTEFACT_DIR=${1:-artefacts}
mkdir -p $ARTEFACT_DIR
checksum=../.properd/${ARTEFACT_DIR}.sha256sum

cd $ARTEFACT_DIR
if test -s $checksum ; then
  sha256sum -c $checksum "./*/*"
else
  sha256sum "*/*"  > $checksum
  echo "checksum: $(readlink -f $checksum)"
fi
